import { NatureBorderDirective } from './nature-border.directive';

describe('NatureBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new NatureBorderDirective();
    expect(directive).toBeTruthy();
  });
});
